#ifndef LB_BRAM_HPP
#define LB_BRAM_HPP

#include "src/utils.hpp"

namespace lb {
   enum class Op : uint8_t;
   enum class ExprTag : uint8_t;
   class Expr;
   enum class Axiom : uint8_t;
   class Rule;
   struct Step;
   struct Proof;
   struct BramFile;
};

#endif
